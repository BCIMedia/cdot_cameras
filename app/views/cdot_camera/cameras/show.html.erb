<div class="body-bg-gradient">
  <div class="row">
    <div class="columns small-12">
      <div class="cdot-container">
        <div class="section-label cdot-header">
          <h3><%= @camera.name %></h3>
        </div>
      <div class="cdot-cards">
        <% @camera.camera_views.each do |view| %>
          <div class="cdot-card">
            <%= image_tag "http://i.cotrip.org/#{view.image_location}", width: 400 %>
            <%= view.description %>
          </div>
        <% end %>
    </div>
  </div>
</div>
